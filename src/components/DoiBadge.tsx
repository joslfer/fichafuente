const DoiBadge = () => {
  return (
    <span
      className="inline-flex items-center rounded-sm border border-foreground/40 px-1 py-0 text-[9px] font-semibold leading-none uppercase tracking-[0.08em] text-foreground"
      aria-label="DOI"
      title="DOI"
    >
      DOI
    </span>
  );
};

export default DoiBadge;
